{
  "geode": "5.7.1",
  "gd": {
    "win": "2.2081",
    "android": "2.2081",
    "mac": "2.2081",
    "ios": "2.2081"
  },
  "id": "anceps.practiceplus",
  "name": "Practice Plus",
  "version": "v1.2.0",
  "developers": ["Anceps", "Vanouper"],
  "description": "Expand Practice like never before :)",

  "dependencies": {
    "hiimjasmine00.optional_settings": ">=v2.0.0",
    "geode.node-ids": ">=v1.23.3",
    "chizz.subtick-inputs-api": ">=v0.4.1"
  },

  "resources": {
    "sprites": ["resources/*.png"],
    "files": [
      "resources/checkpoint_place_sfx.ogg",
      "resources/checkpoint_remove_sfx.ogg"
    ]
  },

  "links": {
    "homepage": "https://practicemodelist.pages.dev",
    "community": "https://discord.gg/4sGSQXFR8N",
    "source": "https://github.com/AncepsGD/Practice-Plus"
  },

  "tags": ["gameplay", "customization", "utility", "offline"],

  "settings": {
    "checkpoint-appearance-title": {
      "name": "Checkpoint Appearance",
      "type": "title"
    },

    "custom-checkpoints-enabled": {
      "name": "Custom Checkpoints",
      "description": "Enable custom checkpoint visuals in practice mode.",
      "type": "bool",
      "default": true
    },

    "checkpoint-shape": {
      "name": "Checkpoint Shape",
      "description": "Shape used for custom checkpoints.",
      "type": "string",
      "default": "diamond",
      "one-of": ["diamond", "circle", "square", "x"],
      "enable-if": "custom-checkpoints-enabled",
      "enable-if-description": "Requires Custom Checkpoints"
    },

    "checkpoint-scale": {
      "name": "Checkpoint Scale",
      "description": "Scale factor for checkpoint visuals.",
      "type": "float",
      "default": 0.7,
      "min": 0.25,
      "max": 4.0,
      "enable-if": "custom-checkpoints-enabled",
      "enable-if-description": "Requires Custom Checkpoints"
    },

    "checkpoint-opacity": {
      "name": "Checkpoint Opacity",
      "description": "Opacity of custom checkpoint visuals.",
      "type": "int",
      "default": 255,
      "min": 25,
      "max": 255,
      "enable-if": "custom-checkpoints-enabled",
      "enable-if-description": "Requires Custom Checkpoints"
    },

    "checkpoint-outer-color-enabled": {
      "name": "Enable Outer Color Override",
      "description": "Use a custom color for the outer checkpoint.",
      "type": "bool",
      "default": true,
      "enable-if": "custom-checkpoints-enabled",
      "enable-if-description": "Requires Custom Checkpoints"
    },

    "checkpoint-outer-color": {
      "name": "Outer Checkpoint Color",
      "description": "Color of the outer checkpoint ring.",
      "type": "color",
      "default": "#FFFFFF",
      "enable-if": "custom-checkpoints-enabled && checkpoint-outer-color-enabled",
      "enable-if-description": "Requires Custom Checkpoints + Outer Color Override"
    },

    "checkpoint-inner-color-enabled": {
      "name": "Enable Inner Color Override",
      "description": "Use a custom color for the inner checkpoint.",
      "type": "bool",
      "default": true,
      "enable-if": "custom-checkpoints-enabled",
      "enable-if-description": "Requires Custom Checkpoints"
    },

    "checkpoint-inner-color": {
      "name": "Inner Checkpoint Color",
      "description": "Color of the inner checkpoint fill.",
      "type": "color",
      "default": "#FF4C00",
      "enable-if": "custom-checkpoints-enabled && checkpoint-inner-color-enabled",
      "enable-if-description": "Requires Custom Checkpoints + Inner Color Override"
    },

    "checkpoint-fade-enabled": {
      "name": "Fade With Placement Count",
      "description": "Each newly placed checkpoint becomes slightly more transparent.",
      "type": "bool",
      "default": false,
      "enable-if": "custom-checkpoints-enabled",
      "enable-if-description": "Requires Custom Checkpoints"
    },

    "checkpoint-fade-step": {
      "name": "Fade Step",
      "description": "Opacity lost for each additional checkpoint.",
      "type": "int",
      "default": 90,
      "min": 0,
      "max": 255,
      "enable-if": "custom-checkpoints-enabled && checkpoint-fade-enabled",
      "enable-if-description": "Requires Custom Checkpoints + Fade With Placement Count"
    },

    "checkpoint-fade-min": {
      "name": "Fade Minimum",
      "description": "Lowest opacity allowed for later checkpoints.",
      "type": "int",
      "default": 0,
      "min": 0,
      "max": 255,
      "enable-if": "custom-checkpoints-enabled && checkpoint-fade-enabled",
      "enable-if-description": "Requires Custom Checkpoints + Fade With Placement Count"
    },

    "checkpoint-fadein-enabled": {
      "name": "Fade-In on Placement",
      "description": "Checkpoints fade in when placed.",
      "type": "bool",
      "default": true,
      "enable-if": "custom-checkpoints-enabled",
      "enable-if-description": "Requires Custom Checkpoints"
    },

    "checkpoint-fadein-duration": {
      "name": "Fade-In Duration",
      "description": "Duration of fade-in animation in seconds.",
      "type": "float",
      "default": 0.2,
      "min": 0.05,
      "max": 3.0,
      "enable-if": "custom-checkpoints-enabled && checkpoint-fadein-enabled",
      "enable-if-description": "Requires Custom Checkpoints + Fade-In"
    },

    "checkpoint-image": {
      "name": "Custom Checkpoint Image",
      "description": "Optional custom texture for checkpoints.",
      "type": "file",
      "default": "",
      "control": {
        "dialog": "open",
        "filters": [
          {
            "files": ["*.png", "*.jpg", "*.jpeg", "*.webp", "*.bmp"],
            "description": "Image files"
          }
        ]
      },
      "enable-if": "custom-checkpoints-enabled",
      "enable-if-description": "Requires Custom Checkpoints"
    },

    "checkpoint-sounds-title": {
      "name": "Checkpoint Sounds",
      "type": "title"
    },

    "checkpoint-sounds-enabled": {
      "name": "Enable Checkpoint Sounds",
      "description": "Toggle checkpoint placement and removal sounds on or off.",
      "type": "bool",
      "default": true
    },

    "checkpoint-sfx-path": {
      "name": "Checkpoint Place Sound",
      "description": "Sound played when placing a checkpoint.",
      "type": "file",
      "default": "{mod_resources_dir}/checkpoint_place_sfx.ogg",
      "enable-if": "checkpoint-sounds-enabled",
      "enable-if-description": "Requires Checkpoint Sounds"
    },

    "checkpoint-remove-sfx-path": {
      "name": "Checkpoint Remove Sound",
      "description": "Sound played when removing a checkpoint.",
      "type": "file",
      "default": "{mod_resources_dir}/checkpoint_remove_sfx.ogg",
      "enable-if": "checkpoint-sounds-enabled",
      "enable-if-description": "Requires Checkpoint Sounds"
    },

    "checkpoint-behavior-title": {
      "name": "Checkpoint Behavior",
      "type": "title"
    },

    "checkpoint-limit": {
      "name": "Checkpoint Limit",
      "description": "Maximum number of practice checkpoints. Disabled if unset.",
      "type": "custom:hiimjasmine00.optional_settings/optional-int",
      "default": 999,
      "default-enabled": true
    },

    "checkpoint-restriction": {
      "name": "Bypass Placement Restrictions",
      "description": "Ignore the game's checkpoint placement restrictions.",
      "type": "bool",
      "default": true
    },

    "practice-mode-title": {
      "name": "Practice Mode",
      "type": "title"
    },

    "practice-overlay-enabled": {
      "name": "Practice Overlay",
      "description": "Show the practice mode HUD overlay.",
      "type": "bool",
      "default": true
    },

    "practice-coins-enabled": {
      "name": "Practice Coins",
      "description": "Allow coins to be collected in practice mode.",
      "type": "bool",
      "default": true
    },

    "checkpoint-behind-player": {
      "name": "Render Behind Player",
      "description": "Render practice checkpoints behind the player.",
      "type": "bool",
      "default": true
    },

    "respawn-delay-title": {
      "name": "Respawn Delay",
      "type": "title"
    },

    "practice-respawn-time-enabled": {
      "name": "Custom Respawn Delay",
      "description": "Enable custom respawn delay in practice mode.",
      "type": "bool",
      "default": true
    },

    "respawn-preset-def-enabled": {
      "name": "Default Respawn",
      "description": "Include the default respawn delay in the cycle.",
      "type": "bool",
      "default": true,
      "enable-if": "practice-respawn-time-enabled",
      "enable-if-description": "Requires Custom Respawn Delay"
    },

    "respawn-preset-zero-enabled": {
      "name": "Instant Respawn",
      "description": "Include instant respawn (0 delay) in the cycle.",
      "type": "bool",
      "default": true,
      "enable-if": "practice-respawn-time-enabled",
      "enable-if-description": "Requires Custom Respawn Delay"
    },

    "respawn-preset-inf-enabled": {
      "name": "No Respawn",
      "description": "Include disabled respawn (infinite delay) in the cycle.",
      "type": "bool",
      "default": true,
      "enable-if": "practice-respawn-time-enabled",
      "enable-if-description": "Requires Custom Respawn Delay"
    },

    "cycle-respawn-time": {
      "name": "Cycle Respawn Time",
      "description": "Cycles through respawn presets.",
      "type": "keybind",
      "default": "C",
      "enable-if": "practice-respawn-time-enabled",
      "enable-if-description": "Requires Custom Respawn Delay"
    }
  }
}
